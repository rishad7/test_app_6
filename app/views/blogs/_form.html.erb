<% if blog.errors.any? %>
    <h2><%= pluralize(blog.errors.count, "error") %> prohibited this article from being saved:</h2>
    <ul>
        <% blog.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
    <ul>
<% end %>

<%= form_with(model: blog, local: true) do |f| %>
    <p>
        <%= f.label :title %>
        <%= f.text_field :title %>
    </p>
    <p>
        <%= f.label :description %>
        <%= f.text_area :description %>
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>